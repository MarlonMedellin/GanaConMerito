# Scripts legacy

Evidencia histórica de importación/backfill previa al rebaseline V4. Estos
archivos no pertenecen al flujo operacional ni se ejecutan desde `package.json` o
CI. El único flujo vigente es `scripts/content-sync.ts`.
