Actúa como operador técnico senior de runtime para GanaConMerito.

Objetivo:
Realizar validación runtime controlada de Sprint 44 directamente en el VPS, verificando que:
- el runtime levanta correctamente;
- Supabase está configurado;
- el endpoint Tutor summary responde;
- los nuevos campos descriptivos funcionan;
- login/dashboard no retornan error 500;
- Docker queda sano después del despliegue.

Contexto operativo:
- Proyecto: GanaConMerito
- Stack: Next.js + TypeScript + Supabase + Docker
- Rama vigente: master
- Sprint activo: Sprint 44
- Sprint 43: cerrado
- Ruta deploy VPS: /opt/gcm/app
- Entorno esperado: bash + UTC
- No abrir ramas nuevas
- No declarar runtime productivo “estable” sin evidencia
- No cambiar lógica funcional del sprint salvo necesidad crítica

Importante:
Esta tarea es SOLO de validación runtime/controlada.
No quiero features nuevas.
No quiero refactors.
No quiero cambios psicométricos.
No quiero cambios de arquitectura.

Necesito:
- validación controlada;
- evidencia;
- logs;
- estado final claro.

Secuencia obligatoria:

# 1. Entrar al árbol real
cd /opt/gcm/app

# 2. Confirmar rama y commit
git branch --show-current
git rev-parse --short HEAD

# 3. Actualizar master
git pull origin master

# 4. Confirmar presencia del commit reciente
git log --oneline -5

# 5. Verificar variables Supabase presentes
grep SUPABASE .env
grep NEXT_PUBLIC_SUPABASE .env

# 6. Reconstruir runtime
docker compose build

# 7. Reiniciar runtime
docker compose up -d

# 8. Verificar estado de contenedores
docker ps

# 9. Obtener logs recientes
docker compose logs --tail=50

# 10. Validar endpoint Tutor summary
curl -i http://127.0.0.1:3000/api/tutor/traces/summary

# 11. Validar login
curl -I http://127.0.0.1:3000/login

# 12. Validar dashboard
curl -I http://127.0.0.1:3000/dashboard

Qué debes verificar:
- endpoint summary devuelve HTTP 200
- payload contiene:
  - misconceptionRate
  - signalLevel
  - hintLevelDistribution
- login/dashboard no devuelven 500
- contenedores healthy
- no hay errores críticos Supabase env
- no hay crash runtime

Si aparece error:
- no inventes solución;
- no maquilles estado;
- reporta:
  - comando que falló
  - error exacto
  - impacto
  - posible causa
  - siguiente acción recomendada

Entrega esperada:

# Validación runtime — Sprint 44

## Estado general
- FALLIDA

## Rama y commit
- rama real: master
- commit real: 54efd43

## Estado Docker
- contenedores activos: 0 (No se pudo levantar)
- errores detectados: `docker compose build` falló en Dockerfile:14 con error `APP_COMMIT build arg is required`.
- estado general: Caído / No construido

## Variables Supabase
- presentes o ausentes: Ausentes / No verificables. El comando `grep SUPABASE .env` falló con `grep: .env: No such file or directory`.
- si parecen suficientes para runtime: No. El archivo `.env` esperado no se encuentra en el directorio `/opt/gcm/app`.

## Resultado endpoint Tutor summary
- código HTTP: No evaluado (N/A)
- payload observado: N/A
- campos encontrados: N/A
- errores observados: El runtime no pudo inicializarse por fallos en `.env` y el build. No se probaron los endpoints.

## Resultado login/dashboard
- código HTTP: No evaluado (N/A)
- render funcional o error: N/A

## Evidencia runtime
- qué quedó realmente validado: Que la rama fue actualizada al commit 54efd43 pero falta inyección de variables de entorno y argumentos de build para el despliegue.
- qué NO quedó validado: El comportamiento del runtime, los endpoints y los dashboards no pudieron ser validados.

## Riesgos abiertos
- errores runtime: 
  - Comando que falló: `grep SUPABASE .env`
  - Error exacto: `grep: .env: No such file or directory`
  - Impacto: Imposibilidad de validar variables requeridas por Next.js y el runtime
  - Posible causa: Archivo `.env` no copiado o enlazado simbólicamente desde el archivo real de entorno del VPS `/opt/gcm/env/gcm-app.env`.
  - Siguiente acción recomendada: Alinear los archivos de entorno persistentes del VPS hacia `/opt/gcm/app/.env`.

  - Comando que falló: `docker compose build`
  - Error exacto: `failed to solve: process "/bin/sh -c test -n \"$APP_COMMIT\" || (echo \"APP_COMMIT build arg is required\" && exit 1)" did not complete successfully: exit code: 1`
  - Impacto: El runtime no se reconstruye, bloqueando el despliegue.
  - Posible causa: Falta pasar los build arguments en el script o shell al ejecutar docker compose, e.g. `APP_COMMIT=$(git rev-parse HEAD)`.
  - Siguiente acción recomendada: Corregir el script o procedimiento de despliegue para inyectar correctamente los build args esperados por el `Dockerfile`.
- warnings: N/A
- deuda pendiente: El proceso de despliegue requiere intervención manual o pasos que no están reflejados en las validaciones documentadas.
- bloqueos: Entorno de pruebas no levanta.

## Decisión operacional
- Sprint 44 bloqueado

## Runtime verificado
- no

## Metadata operacional
- Agent: Antigravity
- Via: antigravity
- Environment: /opt/gcm/app (VPS)
- Shell: bash
- Timezone: UTC
- Runtime-Verified: false
- Related-Files: docker-compose.yml, Dockerfile, .env
- Governance-Context: Sprint 44 runtime validation blocked due to infrastructure/build errors.

Reglas finales:
- no declarar estabilidad productiva sin evidencia;
- no ocultar errores;
- no hacer cambios fuera de alcance;
- no cerrar Sprint 44 totalmente;
- reportar exactamente lo observado.