Quiero cerrar operativamente Sprint 45 en GanaConMerito.

Este trabajo es sobre el VPS actual. Tú ya conoces las carpetas operativas reales del proyecto y las URL activas del entorno, así que úsalas directamente y no me las vuelvas a pedir salvo que encuentres una divergencia real.

Objetivo:
verificar si el estado técnico-documental ya saneado de Sprint 45 está alineado entre fuente, deploy y runtime visible, sin abrir features nuevas.

Reglas obligatorias:
- actualizar primero la carpeta fuente
- alinear después el deploy
- actualizar, reconstruir, reiniciar o verificar Docker según corresponda
- no desarrollar en deploy
- no declarar runtime sin evidencia
- no tocar features ni abrir Sprint 46 todavía
- si fuente, deploy y runtime divergen, corrige primero la fuente

Disciplina operativa:
- usa como referencia la fuente canónica real del proyecto
- usa como referencia el árbol de deploy real del VPS
- usa el archivo de entorno persistente real del deploy
- verifica las URL reales del proyecto que ya conoces
- si detectas drift, repórtalo explícitamente
- no mezcles evidencia de Git con evidencia de runtime
- no cierres por “parece bien”; cierra solo con evidencia

Secuencia esperada:
1. Verifica el estado de la fuente canónica y confirma que Sprint 45 está presente ahí.
2. Actualiza primero la carpeta fuente si hace falta.
3. Verifica el árbol de deploy y confirma si está alineado o no con la fuente.
4. Alinea después el deploy desde Git si corresponde.
5. Actualiza, reconstruye, reinicia o verifica Docker según corresponda.
6. Verifica el runtime visible usando las URL reales del proyecto.
7. Si hay drift entre fuente, deploy y runtime, repórtalo con precisión.
8. No hagas cambios funcionales de producto en esta tarea.

Quiero esta salida exacta:

# Informe de cierre operativo — Sprint 45

## 1. Estado operativo
- cerrado operativo
- cerrado parcial operativo
- no cerrado operativo

## 2. Fuente
- commit observado
- evidencia de que Sprint 45 está en fuente
- si está o no alineada con master

## 3. Deploy
- estado del árbol de deploy
- si está o no alineado con la fuente
- diferencias detectadas

## 4. Docker y servicios
- contenedores o servicios revisados
- estado observado
- acción aplicada si hubo reconstrucción, reinicio o verificación

## 5. Runtime visible
- verificado / no verificado
- URL o URLs revisadas
- evidencia exacta observada
- limitación exacta si no pudo verificarse

## 6. Drift detectado
- fuente vs deploy
- deploy vs runtime
- documentación vs estado real
- corrección aplicada o razón de no corrección

## 7. Validaciones ejecutadas
- comando o chequeo
- resultado
- evidencia resumida

## 8. Criterio final PM
Respóndeme directo:
- ¿Sprint 45 ya puede declararse cerrado total?
- si no, ¿qué falta exactamente?
- ¿ya podemos abrir Sprint 46 o todavía no?

Reglas del informe:
- no inventes evidencia
- no asumas verificación si no ocurrió
- no mezcles deploy con fuente
- no reportes runtime como verificado si no viste evidencia real
- si algo quedó pendiente, escríbelo de forma explícita