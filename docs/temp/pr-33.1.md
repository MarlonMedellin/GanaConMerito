````markdown
TAREA OPERACIONAL COMPLETA EN VPS — GanaConMérito
NO EXPLICAR. NO RESUMIR. EJECUTAR.

OBJETIVO:
Terminar la habilitación operacional real de update.html en producción.

CONTEXTO:
El repositorio YA fue actualizado.
El Dockerfile YA incluye:
- bash
- git
- docker-cli
- docker-cli-compose
- curl

El problema restante es SOLO del VPS/runtime:
- mounts
- rebuild
- validación operacional

====================================================================
1. ACTUALIZAR REPOSITORIOS EN VPS
====================================================================

Ejecutar:

```bash
cd ~/.openclaw/product
git fetch origin --prune
git checkout master
git pull --ff-only origin master

cd /opt/gcm/app
git fetch origin --prune
git checkout master
git reset --hard origin/master
````

Verificar commit actual:

```bash
cd /opt/gcm/app
git rev-parse HEAD
```

Debe contener el commit:
b73a6610599d2428cf7fb4ad7afbfb6ca8a628da
o posterior.

====================================================================
2. EDITAR docker-compose.yml REAL DEL VPS
=========================================

Archivo:

```text
/opt/gcm/docker-compose.yml
```

Ubicar servicio:

```yaml
gcm-app:
```

Agregar EXACTAMENTE estos mounts:

```yaml
volumes:
  - /home/ubuntu/.openclaw/product:/home/ubuntu/.openclaw/product
  - /opt/gcm/app:/opt/gcm/app
  - /opt/gcm/docker-compose.yml:/opt/gcm/docker-compose.yml
  - /var/run/docker.sock:/var/run/docker.sock
```

Agregar EXACTAMENTE estas variables:

```yaml
environment:
  GCM_PRODUCT_DIR: /home/ubuntu/.openclaw/product
  GCM_DEPLOY_DIR: /opt/gcm/app
  GCM_DOCKER_COMPOSE_FILE: /opt/gcm/docker-compose.yml
  GCM_RUNTIME_BASE_URL: http://127.0.0.1:3000
  GCM_QA_BASE_URL: http://127.0.0.1:3000
```

NO romper otras configuraciones existentes.

====================================================================
3. VALIDAR YAML
===============

Ejecutar:

```bash
docker compose -f /opt/gcm/docker-compose.yml config >/tmp/gcm-compose-check.txt
cat /tmp/gcm-compose-check.txt | tail -20
```

Si hay error YAML:

* corregirlo
* volver a validar
* NO continuar hasta que pase

====================================================================
4. REBUILD REAL DEL CONTENEDOR
==============================

Ejecutar:

```bash
cd /opt/gcm/app

export APP_COMMIT=$(git rev-parse HEAD)
export APP_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)

docker compose -f /opt/gcm/docker-compose.yml build gcm-app
docker compose -f /opt/gcm/docker-compose.yml up -d gcm-app
```

Esperar a que termine completamente.

====================================================================
5. VALIDAR TOOLS DENTRO DEL CONTENEDOR
======================================

Ejecutar:

```bash
docker exec gcm-app sh -lc 'which sh'
docker exec gcm-app sh -lc 'git --version'
docker exec gcm-app sh -lc 'docker --version'
docker exec gcm-app sh -lc 'docker compose version'
```

Todos deben responder correctamente.

====================================================================
6. VALIDAR MOUNTS
=================

Ejecutar:

```bash
docker exec gcm-app sh -lc 'test -d /home/ubuntu/.openclaw/product/.git && echo product-ok'

docker exec gcm-app sh -lc 'test -d /opt/gcm/app/.git && echo deploy-ok'

docker exec gcm-app sh -lc 'test -f /opt/gcm/docker-compose.yml && echo compose-ok'

docker exec gcm-app sh -lc 'test -S /var/run/docker.sock && echo docker-sock-ok'
```

Todos deben responder OK.

====================================================================
7. VALIDAR update.html
======================

Abrir:

```text
https://cnsc.profemarlon.com/update.html
```

Usar password:

```text
31129m
```

Ejecutar deploy completo.

Verificar que aparezcan:

```text
preflight
sync-product
sync-deploy
predeploy-tests
docker-deploy
postdeploy-tests
```

y que NO aparezcan:

```text
spawn bash ENOENT
No se encontró un shell ejecutable
GCM_PRODUCT_DIR no visible
docker.sock no accesible
```

====================================================================
8. ENTREGABLE FINAL
===================

Entregar:

* resultado final del deploy
* logs relevantes
* resultado de mounts
* resultado docker tools
* confirmar si update.html quedó plenamente operativo
* indicar cualquier error residual si existe

```
```
