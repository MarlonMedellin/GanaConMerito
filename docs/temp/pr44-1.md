Actúa como operador senior de deploy/runtime para GanaConMerito ejecutándose directamente sobre mi VPS.

Contexto operativo real:
- Proyecto: GanaConMerito
- Rama: master
- Sprint activo: Sprint 44
- Stack: Next.js + TypeScript + Supabase + Docker
- Runtime principal: Docker Compose
- Entorno real: VPS
- El repositorio NO se despliega desde `/opt/gcm/app`
- La carpeta fuente real es:
  `/home/ubuntu/.openclaw/product`
- El deploy reconstruye runtime desde esa carpeta
- El entorno `.env` persistente NO vive necesariamente dentro del repo
- Puede existir en:
  `/opt/gcm/env/gcm-app.env`
- Anti-gravity ya se ejecuta dentro del VPS
- No quiero simulaciones
- No quiero cambios de arquitectura
- No quiero inventar fixes
- No quiero refactors
- Quiero validación runtime real y controlada

Objetivo:
1. Descargar/actualizar la última versión del repo en `/home/ubuntu/.openclaw/product`
2. Ejecutar deploy real
3. Reconstruir Docker correctamente
4. Levantar runtime
5. Ejecutar validaciones runtime reales
6. Validar Sprint 44 en runtime controlado
7. Determinar si Sprint 44 queda:
   - ampliamente completado
   - o listo para cierre parcial

Importante:
NO asumas que `.env` vive dentro del repo.
Primero debes identificar cómo el docker-compose real consume variables de entorno en el VPS.

Flujo obligatorio:

# 1. Entrar a carpeta fuente real
cd /home/ubuntu/.openclaw/product

# 2. Confirmar rama y commit actual
git branch --show-current
git rev-parse --short HEAD

# 3. Actualizar master
git fetch origin
git pull origin master

# 4. Confirmar últimos commits
git log --oneline -5

# 5. Inspeccionar estructura real de deploy
pwd
ls -la
ls -la /opt/gcm
ls -la /opt/gcm/env

# 6. Inspeccionar cómo docker-compose consume variables
cat docker-compose.yml

# 7. Verificar variables reales del entorno
NO asumir `.env` local.
Buscar referencias reales:
- env_file
- environment
- gcm-app.env
- variables exportadas

# 8. Confirmar existencia del archivo real de entorno
Ejemplos posibles:
- /opt/gcm/env/gcm-app.env
- otros paths encontrados

# 9. Ejecutar rebuild real
docker compose build

# 10. Levantar runtime
docker compose up -d

# 11. Verificar estado docker
docker ps

# 12. Revisar logs
docker compose logs --tail=100

# 13. Validación runtime interna
Validar internamente:
- endpoint Tutor summary
- login
- dashboard
- payload analytics Sprint 44

Ejemplos:
curl -i https://cnsc.profemarlon.com/api/tutor/traces/summary
curl -I https://cnsc.profemarlon.com/login
curl -I https://cnsc.profemarlon.com/dashboard

# 14. Validación visual/runtime externa
Si el entorno responde:
usar Chromium Playwright para:
- abrir login
- abrir dashboard
- verificar que la UI renderice
- verificar que no existan pantallas 500
- verificar que el card/resumen Tutor renderice si está disponible

NO automatizar navegación compleja.
Solo validación runtime mínima y real.

# 15. Verificar Sprint 44 runtime
Confirmar si aparecen:
- signalLevel
- misconceptionRate
- hintLevelDistribution

# 16. Ejecutar validaciones repo
npm run check:doc-triggers
npm run lint
npm test

Reglas:
- no modificar Dockerfile salvo error crítico explícito
- no inventar variables
- no ocultar errores
- no declarar runtime sano sin evidencia
- no declarar Sprint 44 cerrado total
- si el build falla, reportar exactamente:
  - paso
  - error
  - impacto
  - causa probable
  - siguiente acción recomendada

Entrega esperada:

# Validación VPS — Sprint 44

## Estado general
- FALLIDA

## Rama y commit
- rama real: master
- commit real: 54efd43

## Estado deploy
- repo actualizado o no: Sí, en `/home/ubuntu/.openclaw/product`
- deploy ejecutado o no: No
- rebuild docker exitoso o no: No

## Estado Docker
- contenedores: Ninguno activo (No levantó)
- health: N/A
- errores: `docker compose build` falló en el contexto `/opt/gcm/app` por falta del build arg `APP_COMMIT`.

## Variables entorno
- cómo se resolvieron: El docker-compose real en `/opt/gcm` usa `env_file: - /opt/gcm/env/gcm-app.env`.
- archivo real usado: `/opt/gcm/env/gcm-app.env`
- si runtime pudo consumirlas: No aplicable, la falla de build bloqueó la inicialización.

## Validación runtime interna
- endpoint summary: No evaluado
- login: No evaluado
- dashboard: No evaluado
- payload analytics: No evaluado

## Validación Chromium Playwright
- páginas abiertas: N/A
- render correcto o no: N/A
- errores visuales/runtime: N/A

## Sprint 44 runtime
- signalLevel presente o no: N/A
- misconceptionRate presente o no: N/A
- hintLevelDistribution presente o no: N/A

## Validaciones repo
- check:doc-triggers: No ejecutado
- lint: No ejecutado
- test: No ejecutado

## Riesgos abiertos
- runtime: 
  - paso: Ejecución de `docker compose build` en `/opt/gcm`
  - error: `failed to solve: process "/bin/sh -c test -n \"$APP_COMMIT\" || (echo \"APP_COMMIT build arg is required\" && exit 1)" did not complete successfully: exit code: 1`
  - impacto: El runtime de docker falla al construirse y levantar, por lo cual el entorno productivo no puede aplicar la versión nueva de Sprint 44.
  - causa probable: El `docker-compose.yml` de `/opt/gcm` no inyecta los `args` (build args) requeridos por el `Dockerfile` de `/opt/gcm/app` (en este caso, `APP_COMMIT` y `APP_BUILD_TIME`).
  - siguiente acción recomendada: Modificar el `docker-compose.yml` para mapear los `build args` o proveerlos a través del entorno antes de ejecutar el build (`APP_COMMIT=... APP_BUILD_TIME=... docker compose build`).
- docker: Imagen sin construir, `APP_COMMIT` es estricto en el Dockerfile.
- env: Se identificó correctamente `/opt/gcm/env/gcm-app.env` pero no llega a consumirse por Next.js porque el build revienta antes.
- drift: La carpeta `/opt/gcm/app` define el `Dockerfile` pero no se actualiza o no tiene las variables de entorno inyectadas.
- warnings: N/A

## Decisión operacional
- Sprint 44 bloqueado

## Runtime verificado
- no

## Metadata operacional
- Agent: Antigravity
- Via: antigravity
- Environment: VPS (`ganaconmerito`)
- Shell: bash
- Timezone: UTC
- Runtime-Verified: false
- Related-Files: `/opt/gcm/docker-compose.yml`, `/opt/gcm/app/Dockerfile`, `/home/ubuntu/.openclaw/product`
- Governance-Context: Validación de Sprint 44 fallida en tiempo de build por requerimientos de CI/CD estrictos no satisfechos en docker-compose.

Regla final:
No maquilles estado.
Quiero evidencia runtime real del VPS, no inferencias.