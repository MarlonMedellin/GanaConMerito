Actúa como agente de operación y QA para GanaConMerito.

Sprint 12 — Métricas confiables y útiles v1.

Objetivo:
Desplegar en producción el merge final del PR #6 y ejecutar una prueba E2E online que valide que el dashboard comunica métricas de forma prudente, sin vender humo.

Repositorio:
https://github.com/ProfeMarlonMDE/GanaConMerito

Rama:
master

Commit esperado en master:
[64d78def1d8dd4f98ec9ae5ba55a3fed97e4e4ba]

Short hash esperado:
[64d78def1d8dd4f98ec9ae5ba55a3fed97e4e4ba]

Alcance:
- Sí actualizar VPS.
- Sí actualizar /opt/gcm/app.
- Sí reconstruir Docker.
- Sí ejecutar prueba online contra https://cnsc.profemarlon.com.
- No modificar código.
- No tocar Supabase Dashboard.
- No tocar scoring.
- No tocar Tutor GCM.
- No tocar selección de preguntas.
- No crear commits salvo documentación de cierre si se solicita explícitamente.

Tareas de despliegue:

ssh ganaconmerito "
set -e

EXPECTED_COMMIT='[PEGAR_HASH_FINAL]'

echo '--- Updating source folder ---'
cd ~/.openclaw/product
git fetch origin
git checkout master
git reset --hard \$EXPECTED_COMMIT
SOURCE_HEAD=\$(git rev-parse HEAD)
echo SOURCE_HEAD=\$SOURCE_HEAD

echo '--- Updating deploy folder ---'
sudo git -C /opt/gcm/app fetch origin
sudo git -C /opt/gcm/app checkout master
sudo git -C /opt/gcm/app reset --hard \$EXPECTED_COMMIT
DEPLOY_HEAD=\$(git -C /opt/gcm/app rev-parse HEAD)
echo DEPLOY_HEAD=\$DEPLOY_HEAD

test \"\$SOURCE_HEAD\" = \"\$DEPLOY_HEAD\"
echo SOURCE_DEPLOY_MATCH=OK

echo '--- Building Docker ---'
APP_COMMIT=\$(git -C /opt/gcm/app rev-parse --short HEAD)
APP_BUILD_TIME=\$(date -u +%Y-%m-%dT%H:%M:%SZ)

cd /opt/gcm
sudo docker compose -f docker-compose.yml build \
  --build-arg APP_COMMIT=\"\$APP_COMMIT\" \
  --build-arg APP_BUILD_TIME=\"\$APP_BUILD_TIME\" \
  gcm-app

echo '--- Restarting gcm-app ---'
sudo docker compose -f docker-compose.yml up -d gcm-app

echo '--- Runtime status ---'
sudo docker ps | grep gcm-app
sudo docker logs --tail 30 gcm-app
"

Después ejecutar E2E online.

Usar sesión autenticada existente:
artifacts/auth-state.json

Si la sesión expiró, ejecutar primero:
E2E_BASE_URL=https://cnsc.profemarlon.com npx playwright test tests/e2e/auth.setup.ts --project=chromium --headed --reporter=list

Crear o actualizar esta prueba:
tests/e2e/online-dashboard-metric-signal-contract.spec.ts

La prueba debe validar:

1. Runtime
- abrir /login
- verificar commit visible igual al short hash esperado
- capturar build time
- screenshot

2. Sesión autenticada
- abrir /home
- confirmar que no redirige a /login
- screenshot

3. Dashboard histórico
- abrir /dashboard
- confirmar que carga
- capturar texto visible
- screenshot full page

4. Contrato de copy prudente
Validar que el dashboard NO muestre promesas fuertes no soportadas:
- no debe aparecer “probabilidad de aprobar”
- no debe aparecer “estás listo”
- no debe aparecer “vas a ganar”
- no debe aparecer “nivel real frente a otros”
- no debe aparecer “percentil estimado”

5. Señal y prudencia
Debe aparecer al menos uno de estos estados:
- Sin señal
- Señal inicial
- Señal emergente
- Señal usable

Debe aparecer alguna frase prudente si hay poca muestra:
- “muestra”
- “señal”
- “no concluyente”
- “prudencia”
- “orientación”
- “evidencia”

6. Métricas visibles
Confirmar que existen:
- Precisión
- Intentos
- Razonamiento promedio
- Señal de nivel o Nivel estimado
- Lectura ejecutiva
- Focos de refuerzo
- Detalle por tema

7. Percentil
Si aparece “Percentil”, verificar:
- si hay baja señal, debe decir “Lectura no concluyente” o equivalente.
- no debe inventar percentil si no hay dato sólido.

8. Fortalezas/refuerzos
Validar que no se presenten como conclusión fuerte si no hay evidencia:
- debe aceptar textos como “Fortaleza aún no concluyente”
- debe aceptar textos como “Refuerzo sugerido inicial”
- debe aceptar “Sin conclusión todavía”
- debe aceptar “Sin prioridad concluyente”

9. Flujo de práctica
- abrir /practice
- iniciar práctica
- responder mínimo 3 preguntas, ideal 5
- escribir justificación en cada pregunta
- verificar /api/session/advance 200
- screenshot de feedback

10. Dashboard de sesión
- abrir enlace “Ver sesión” si está disponible, o /dashboard?sessionId=<sessionId>
- confirmar que carga
- verificar que las métricas también usan señal prudente
- screenshot

11. Logout
- cerrar sesión
- confirmar /login
- abrir /practice
- confirmar que queda protegido
- screenshot

12. Red y consola
Registrar:
- errores 5xx
- 4xx críticos
- console errors críticos
- status de /api/session/start
- status de /api/session/item
- status de /api/session/advance

Artefactos:
Crear carpeta:
artifacts/online-dashboard-metric-signal-contract/

Generar:
- dashboard-metric-signal-report.json
- screenshots:
  - 01-runtime.png
  - 02-home.png
  - 03-dashboard-historical.png
  - 04-practice-feedback.png
  - 05-dashboard-session.png
  - 06-after-logout.png
  - 07-post-logout-protected.png

JSON obligatorio:
- baseUrl
- expectedCommit
- runtimeCommitVisible
- buildTimeVisible
- dashboardTextSample
- signalLabelsDetected
- prudentCopyDetected
- forbiddenClaimsDetected
- metricsVisible
- percentileStatus
- strengthsCopyStatus
- weaknessesCopyStatus
- questionsAnswered
- sessionDashboardStatus
- logoutStatus
- postLogoutProtection
- networkErrors5xx
- networkErrors4xxCritical
- consoleErrorsCritical
- screenshots
- verdict: PASS / WARN / FAIL

Criterio PASS:
- runtime commit visible correcto
- dashboard carga
- aparece signalLabel
- no aparecen promesas o claims falsos
- métricas principales visibles
- percentil no se presenta como fuerte sin base
- fortalezas/refuerzos usan copy prudente cuando corresponde
- práctica sigue funcionando
- dashboard de sesión funciona
- logout funciona
- post-logout protegido
- sin 5xx
- sin 4xx críticos

Criterio WARN:
- funcionalmente correcto, pero algún copy queda ambiguo
- hay señal global usable pero no hay competencia fuerte específica
- latencia alta controlada
- solo se responden 3 preguntas

Criterio FAIL:
- runtime no corresponde al commit esperado
- dashboard no carga
- aparecen claims tipo “probabilidad de aprobar” o “estás listo”
- se inventa percentil
- práctica se rompe
- logout falla
- hay 5xx o 4xx crítico

Comando:
E2E_BASE_URL=https://cnsc.profemarlon.com EXPECTED_COMMIT=[SHORT_HASH_FINAL] npx playwright test tests/e2e/online-dashboard-metric-signal-contract.spec.ts --project=chromium --reporter=list

Entrega final:
1. PASS / WARN / FAIL
2. runtime commit visible
3. build time visible
4. signalLabels detectados
5. forbiddenClaims detectados
6. métricas visibles
7. estado del percentil
8. estado fortalezas/refuerzos
9. preguntas respondidas
10. dashboard histórico OK/WARN/FAIL
11. dashboard sesión OK/WARN/FAIL
12. logout OK/FAIL
13. protección post-logout OK/FAIL
14. errores 5xx
15. 4xx críticos
16. console errors críticos
17. screenshots
18. ruta del JSON
19. conclusión operativa